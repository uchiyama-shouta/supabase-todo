import { FC } from "react";
const Spinner: FC = () => {
  return (
    <div className="my-5 w-8 h-8 rounded-full border-2 border-pink-600 border-t-transparent animate-spin"></div>
  );
};

export default Spinner;
